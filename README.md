# movemark-ios

## Supabase (local build)

Copy `Config/Secrets.xcconfig.example` to `Config/Secrets.xcconfig` and set `SUPABASE_URL` and `SUPABASE_ANON_KEY`. The latter file is gitignored and is included by `MoveMarkApp.xcconfig`.
